services:
  portainer:
    image: portainer/portainer-ce:lts
    container_name: portainer
    restart: always
    network_mode: bridge
    ports:
      - "8000:8000"
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /data/portainer:/data
