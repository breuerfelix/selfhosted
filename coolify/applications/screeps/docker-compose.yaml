# Generated from the committed Coolify export for screeps.
# Values intentionally remain redacted where the source export redacted them.
x-coolify:
  name: "screeps"
  uuid: "u8ckco8wk8o84wgw00s0044o"
  fqdn: "https://screeps.felixbreuer.me"
  redirect: "both"
  source_image: "screepers/screeps-launcher:latest"
  compose_parsing_version: "5"
  custom_labels_omitted: true
  env_values_redacted: false
services:
  screeps:
    image: "screepers/screeps-launcher:latest"
    restart: unless-stopped
    expose:
      - "21025"
    volumes:
      - screeps-data:/screeps
    # If the live stack still externalizes /screeps/config.yml, add a read-only bind mount before deploy.
volumes:
  # Legacy Kubernetes manifest used PVC screeps-pvc (3Gi) mounted at /screeps.
  screeps-data: {}
